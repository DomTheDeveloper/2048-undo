# Compatibility entrypoint. The separate lean/ Mathlib prototype is preserved,
# but its recursion-depth failures are not evidence for the manuscript's claims.
# Both entrypoints now rebuild the canonical stronger kernel-only development.
name: Lean 2048 proof
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  verify:
    uses: ./.github/workflows/lean-2048-kernel.yml
